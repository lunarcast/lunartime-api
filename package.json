{
    "version": "1.0.0",
    "engines": {
        "node": ">=14.8.0"
    },
    "scripts": {
        "build": "tsc",
        "dev": "tsnd --respawn --notify=false -T -r dotenv/config src"
    },
    "devDependencies": {
        "@types/node": "^14.14.5",
        "prettier": "^2.1.2",
        "ts-node-dev": "^1.0.0",
        "typescript": "^4.0.5"
    },
    "dependencies": {
        "@prisma/cli": "^2.10.0",
        "@prisma/client": "^2.10.0",
        "dotenv": "^8.2.0"
    }
}
